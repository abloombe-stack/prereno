# Supabase - Get from your Supabase project settings
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=  # Server-only

# Stripe - Get from your Stripe dashboard
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=          # Server-only
STRIPE_WEBHOOK_SECRET=      # Server-only, add after webhook creation

# Email (Resend) - Get from resend.com
RESEND_API_KEY=             # Server-only
RESEND_DOMAIN=              # Server-only, e.g., mail.prereno.com

# SMS (Twilio) - Get from twilio.com
TWILIO_ACCOUNT_SID=         # Server-only
TWILIO_AUTH_TOKEN=          # Server-only
TWILIO_FROM_NUMBER=         # Server-only, e.g., +1XXXXXXXXXX

# AI - Get from openai.com
OPENAI_API_KEY=             # Server-only

# Auth - Generate a random 32+ character string
JWT_SECRET=                 # Server-only

# Frontend
VITE_PUBLIC_URL=http://localhost:5173